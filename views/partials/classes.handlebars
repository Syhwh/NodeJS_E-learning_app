<div class="container">
    <div class="row">
    {{#each classes}}
    <div class="col-md-4">
        <h2>{{title}} </h2>
        <p> {{description}} </p>
        <p><a class="btn btn-secondary" href="/classes/{{_id}}/details" role="button">View details &raquo;</a></p>
    </div>
    {{/each}}
  </div>
</div>